#!/usr/bin/env bash

function main {
  local shome="$(cd -P -- "${BASH_SOURCE%/*}/.." && pwd -P)"
  source "$shome/script/profile"

  export PYTHONPATH="$shome/vendor/blessclient/blessclient"
  exec "$shome/vendor/virtualenv/bin/blessclient" "$@"
}

main "$@"
